# Backend URL used by Vite's dev proxy (Node only, not exposed to the browser)
BACKEND_URL=

# Backend URL exposed to the browser bundle
# Leave empty in development (Vite proxy handles /api and /uploads)
# Set the full URL in production: https://api.myapp.com
VITE_API_URL=
